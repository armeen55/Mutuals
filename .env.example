# Copy to .env.local and fill in to enable REAL cross-device multiplayer.
# Without these, MUTUALS runs on the localStorage fallback (single-browser, demo-safe).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
